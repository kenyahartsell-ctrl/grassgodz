{
  "name": "Payment",
  "type": "object",
  "properties": {
    "job_id": {
      "type": "string"
    },
    "customer_id": {
      "type": "string"
    },
    "provider_id": {
      "type": "string"
    },
    "stripe_payment_intent_id": {
      "type": "string"
    },
    "amount": {
      "type": "number"
    },
    "platform_fee": {
      "type": "number"
    },
    "payout_amount": {
      "type": "number"
    },
    "status": {
      "type": "string",
      "enum": [
        "authorized",
        "captured",
        "refunded",
        "failed"
      ],
      "default": "authorized"
    },
    "description": {
      "type": "string",
      "description": "Optional notes or description about this payment",
      "maxLength": 1000
    }
  },
  "required": [
    "job_id",
    "amount"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "data.customer_id": "{{user.id}}"
        },
        {
          "data.provider_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}