{
  "name": "Job",
  "type": "object",
  "properties": {
    "customer_id": {
      "type": "string",
      "description": "CustomerProfile ID"
    },
    "customer_email": {
      "type": "string"
    },
    "customer_name": {
      "type": "string"
    },
    "provider_id": {
      "type": "string",
      "description": "ProviderProfile ID, nullable until claimed"
    },
    "provider_email": {
      "type": "string"
    },
    "provider_name": {
      "type": "string"
    },
    "service_id": {
      "type": "string"
    },
    "service_name": {
      "type": "string"
    },
    "yard_size": {
      "type": "string",
      "enum": [
        "small",
        "medium",
        "large",
        "xl",
        "commercial_small",
        "commercial_large"
      ],
      "description": "Size of the yard/property for pricing purposes"
    },
    "address": {
      "type": "string"
    },
    "zip_code": {
      "type": "string"
    },
    "latitude": {
      "type": "number",
      "description": "Set when job is created via geocoding"
    },
    "longitude": {
      "type": "number",
      "description": "Set when job is created via geocoding"
    },
    "scheduled_date": {
      "type": "string",
      "format": "date"
    },
    "scheduled_time": {
      "type": "string",
      "description": "HH:MM scheduled start time for the job (24h format)"
    },
    "recurrence": {
      "type": "string",
      "enum": [
        "one_time",
        "weekly",
        "biweekly"
      ],
      "default": "one_time",
      "description": "How often this recurring service should repeat"
    },
    "recurrence_parent_id": {
      "type": "string",
      "description": "ID of the original job this was auto-generated from (for recurring jobs)"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending_deposit",
        "pending_payment",
        "requested",
        "quoted",
        "accepted",
        "scheduled",
        "in_progress",
        "completed",
        "cancelled"
      ],
      "default": "requested"
    },
    "customer_notes": {
      "type": "string"
    },
    "provider_notes": {
      "type": "string"
    },
    "customer_yard_photos": {
      "type": "array",
      "description": "Photos of the yard uploaded by the customer at booking time",
      "items": {
        "type": "string"
      }
    },
    "quoted_price": {
      "type": "number"
    },
    "final_price": {
      "type": "number"
    },
    "platform_fee": {
      "type": "number"
    },
    "provider_payout": {
      "type": "number"
    },
    "completed_at": {
      "type": "string",
      "format": "date-time"
    },
    "completion_photos": {
      "type": "object",
      "description": "Before/after photos submitted by the provider.",
      "properties": {
        "front_before": {
          "type": "string"
        },
        "front_after": {
          "type": "string"
        },
        "back_before": {
          "type": "string"
        },
        "back_after": {
          "type": "string"
        },
        "left_before": {
          "type": "string"
        },
        "left_after": {
          "type": "string"
        },
        "right_before": {
          "type": "string"
        },
        "right_after": {
          "type": "string"
        }
      }
    },
    "admin_photos": {
      "type": "array",
      "description": "Photos uploaded by admin after job completion.",
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          },
          "uploaded_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    },
    "cancelled_by": {
      "type": "string",
      "enum": [
        "customer",
        "provider",
        "admin",
        "system"
      ]
    },
    "cancelled_at": {
      "type": "string",
      "format": "date-time"
    },
    "cancellation_reason": {
      "type": "string"
    },
    "cancellation_fee_charged": {
      "type": "number",
      "default": 0
    },
    "provider_no_show": {
      "type": "boolean",
      "default": false
    },
    "customer_no_show": {
      "type": "boolean",
      "default": false
    },
    "weather_check_status": {
      "type": "string",
      "enum": [
        "pending",
        "safe",
        "caution",
        "unsafe"
      ]
    },
    "weather_check_at": {
      "type": "string",
      "format": "date-time"
    },
    "auto_cancelled_weather": {
      "type": "boolean",
      "default": false
    },
    "customer_weather_override": {
      "type": "boolean",
      "default": false
    },
    "subscription_id": {
      "type": "string"
    },
    "is_cash_job": {
      "type": "boolean",
      "default": false
    },
    "payment_method": {
      "type": "string",
      "enum": [
        "stripe",
        "cash"
      ],
      "default": "stripe"
    },
    "deposit_required": {
      "type": "boolean",
      "default": false,
      "description": "True if job total exceeds $200 and a 50% deposit is required"
    },
    "deposit_amount": {
      "type": "number",
      "description": "50% of quoted price, required before job is visible to providers"
    },
    "deposit_paid": {
      "type": "boolean",
      "default": false,
      "description": "True once deposit payment is confirmed"
    },
    "deposit_payment_intent_id": {
      "type": "string",
      "description": "Stripe payment intent ID for the deposit transaction"
    },
    "remaining_balance": {
      "type": "number",
      "description": "Full price minus deposit amount \u2014 charged at job completion"
    },
    "final_payment_intent_id": {
      "type": "string",
      "description": "Stripe payment intent ID for the remaining balance payment"
    },
    "cash_paid": {
      "type": "boolean",
      "default": false,
      "description": "True when customer has paid the provider in cash"
    },
    "cash_paid_date": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when provider marked cash payment received"
    },
    "quote_nudge_sent_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the 24-hour quote nudge notification was sent to the customer"
    },
    "pending_payment_link": {
      "type": "string",
      "description": "Stripe Checkout URL shown when customer has no email on file"
    },
    "admin_payment_status": {
      "type": "string",
      "enum": [
        "paid",
        "payment_pending"
      ],
      "description": "Admin-controlled payment status override for completed jobs. Does not affect Stripe fields."
    },
    "provider_paid_out": {
      "type": "boolean",
      "default": false,
      "description": "True when the provider has been paid out by admin"
    },
    "provider_payout_method": {
      "type": "string",
      "enum": [
        "cash",
        "card",
        "bank_transfer",
        "other"
      ],
      "description": "Method used to pay out the provider"
    },
    "provider_payout_date": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when provider was paid out"
    },
    "provider_payout_photos": {
      "type": "array",
      "description": "Photos as proof of provider payout (e.g. cash handoff, Zelle screenshot).",
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string"
          },
          "uploaded_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    },
    "provider_payout_notes": {
      "type": "string",
      "description": "Admin notes about the provider payout"
    },
    "accepted_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the provider accepted/claimed the job. Used to enforce the 2-hour arrival window."
    },
    "acceptance_timer_flagged": {
      "type": "boolean",
      "default": false,
      "description": "True when the 2-hour acceptance window expired with no provider action (no cancellation, no in-progress). Flags job for admin review."
    },
    "acceptance_timer_flagged_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the acceptance timer flag was set."
    },
    "provider_cancellation_count": {
      "type": "number",
      "default": 0,
      "description": "Number of times this job has been cancelled by a provider after acceptance. Tracked for accountability."
    },
    "excluded_from_payout": {
      "type": "boolean",
      "default": false,
      "description": "Flag to manually exclude this job from provider payout lists"
    },
    "declined_by": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of ProviderProfile IDs that have declined this job. Used to hide the job from providers who already declined it."
    }
  },
  "required": [
    "customer_id",
    "service_id",
    "address",
    "zip_code"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.customer_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.customer_email": "{{user.email}}"
        },
        {
          "data.provider_email": "{{user.email}}"
        },
        {
          "data.provider_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.customer_email": "{{user.email}}"
        },
        {
          "data.provider_email": "{{user.email}}"
        },
        {
          "data.provider_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}